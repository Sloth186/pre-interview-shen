# Pre-interview
Shen Rothermel

I have been to 9 different schools through K-12.

For Task 3, run "./run-queries.sh [...]" where [...] is replaced by the directory where all the queries are contained.

For Task 4, run "./run-queries-data.sh [...]" where [...] is replaced by the directory where all the queries are contained. After execution finishes, a csv file named data should be produced. Open this for the desired information.